# (TM101) トレーサビリティ・マトリクス

> [!NOTE]
> 本テンプレートは**推奨ツール**です。DADAプロセスの必須ステップではありませんが、要求・設計・テスト間の対応関係を可視化し、漏れや乖離を防ぐために活用を推奨します。

【運用ルール】
1. **要求定義（Phase 1）完了後**に、要求ID列を埋めます。
2. **設計（Phase 2）完了後**に、対応する設計ユニットID列を埋めます。
3. **テスト仕様（Phase 3）完了後**に、対応するテストID列を埋め、網羅性を確認します。
4. 各列が空欄のままの行があれば、**対応漏れ（未実装・未テスト）** の可能性があります。

## マトリクス

| 要求ID (SW105) | 要求概要 | 設計ユニットID (SW205) | テストID (SWP6) | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| **4.2.1** | 起動およびローディング処理 | `AppController` (3.2.1) | TC-NOR-001 | 100ms以内の起動、および通信完了までのローディング描画。 |
| **4.2.2** | 為替データの非同期取得 | `ExchangeRateService` (3.2.2) | TC-NOR-002, TC-ERR-002 | HTTPS非同期 fetch、および 5秒タイムアウト (AbortController) の監視・例外処理。 |
| **4.2.3** | 最新レートの表示 | `AppController` (3.2.1) | TC-NOR-003 | 小数点以下3桁ゼロパディング表示。 |
| **4.2.4** | 価格推移グラフ描画 | `GraphRenderer` (3.2.4, 5.3) | TC-NOR-004, TC-NOR-007, TC-NOR-008, TC-ERR-009 | SVGによる滑らかな曲線とグラデーション、右端50pxのY軸領域、最高値・最安値、現在値インジケータ（◀矢印＋数値ラベル）の動的描画、およびゼロ除算や範囲外データのガード処理。 |
| **4.2.5** | エラーハンドリング・オフライン | `AppController` (3.2.1)<br>`StorageService` (3.2.3)<br>7章 例外処理一覧 | TC-ERR-001, TC-ERR-004, TC-ERR-008 | ネットワーク障害時のキャッシュ（24時間以内）フォールバック表示、完全エラー表示（キャッシュなし/期限切れ時）、および再試行ボタンの動作。 |
| **6.1** | 性能・効率性 | 4.3 性能見積り | TC-NFC-002, TC-NFC-004 | JavaScript/SVG描画10ms以内、全体1.2s以内、およびパッケージサイズ1MB未満。 |
| **6.2** | 信頼性・安全性 (整合性) | `ExchangeRateService` (3.2.2)<br>`StorageService` (3.2.3)<br>7章 例外処理一覧 | TC-NOR-005, TC-ERR-003, TC-ERR-005, TC-ERR-006, TC-ERR-007 | キャッシュのシリアライズ保存、24時間期限判定、キャッシュ破損、およびAPIから受信した異常値（NaN、ゼロ、負値）の境界値チェックとフォールバック。 |
| **6.3** | 保守性・拡張性 (他通貨) | `ExchangeRateService` (3.2.2)<br>`StorageService` (3.2.3) | TC-NOR-006 | 通査ペア引数 `pair` のサポート、動的なキャッシュストレージキーの分離設計。 |
| **6.4** | セキュリティ | 8.2 ADR 01 (SVG自作描画) | TC-NFC-001 | CSP制約に抵触しない Vanilla JS / SVG 自作描画。 |
| **-** | メモリリソース管理 | 4.1 メモリ構成とレイアウト | TC-NFC-003 | ポップアップ閉鎖時の実行コンテキスト消滅に伴うメモリ完全解放。 |

## 網羅性チェック

- [x] すべての要求ID（SW105）に対応する設計ユニットID（SW205）が存在する
- [x] すべての要求ID（SW105）に対応するテストID（SWP6）が存在する
- [x] 設計ユニットIDに対応する要求が不明な項目がない（設計の独走がない）
